spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: discodeit

discodeit:
  repository:
    type: file # file | jcf
    file-directory: .discodeit